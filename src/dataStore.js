// In-memory data store
let posts = [];
let users = [];
let nextId = 1;
let nextCommentId = 1;
let nextUserId = 1;

module.exports = {
    posts,
    users,
    nextId,
    nextCommentId,
    nextUserId
};
